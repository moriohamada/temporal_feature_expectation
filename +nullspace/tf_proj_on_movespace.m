function tf_proj_on_movespace(projs_iters_aligned, dims_iters_aligned, t_ax_resps, ops)
% 
% Visualize fast and slow preferring units activities when projected onto movespace iters (or
% equivalently, the contribution of fast/slow/non-pref units to each of the movespace dimensions.
% 
% --------------------------------------------------------------------------------------------------

% calculate resps for each iter to TF pulses
for iter = 1:numel(projs_iters_aligned)
    
    
    
end

end