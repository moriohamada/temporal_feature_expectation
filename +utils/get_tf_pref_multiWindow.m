function [tf_sensitivity, tf_preferences] = get_tf_pref_multiWindow(indexes)

% get tf preferences in multiple windows

win_names = {'early_brief', ...
             'early_longer', ...
             'later_med', ...
             'later_long', ...
             'control'};
         
for w = 1:length(win_names)
    
end

end